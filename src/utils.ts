import { NewDiaryEntry } from "./types"

const toNewDiaryEntry = (object: any): NewDiaryEntry => {
  const newEntry: NewDiaryEntry = {
    // There is gonna be logic here
  }

  return newEntry
}

export default toNewDiaryEntry
